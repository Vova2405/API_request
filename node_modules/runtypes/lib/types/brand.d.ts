import { Runtype, Static } from '../runtype';
export declare const RuntypeName: unique symbol;
export interface RuntypeBrand<B extends string> {
    [RuntypeName]: B;
}
export interface Brand<B extends string, A extends Runtype> extends Runtype<Static<A> & RuntypeBrand<B>> {
    tag: 'brand';
    brand: B;
    entity: A;
}
export declare function Brand<B extends string, A extends Runtype>(brand: B, entity: A): Brand<B, A>;
